import 'package:flutter/rendering.dart';

class RadiusValue {
  static const BorderRadiusGeometry smallRadius =
      BorderRadius.all(Radius.circular(10));
  static const BorderRadiusGeometry mediumRadius =
      BorderRadius.all(Radius.circular(15));
  static const BorderRadiusGeometry bigRadius =
      BorderRadius.all(Radius.circular(20));
}
